meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{apiURL}}/Mobileapi/auth
  body: json
  auth: inherit
}

body:json {
  {
    "username" : "501",
    "password" : "Mobile@12"
  }
}

script:post-response {
  test("Checking for status code 200", function () {
      expect(res.getStatus()).to.equal(200);
  });
  
}

settings {
  encodeUrl: true
}
